---
name: User profile
description: User is a Node.js/TypeScript developer building a CLI tool; prefers no external dependencies for audio
type: user
---

User builds Node.js/TypeScript projects. Current project is "pina", a personal CLI project manager using Ink (React for terminal UIs). They use `tsx` for running TypeScript scripts directly and `tsup` for bundling.

For audio/sound work: strongly prefers zero external dependencies — raw Buffer manipulation over any audio library.
